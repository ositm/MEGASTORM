{
  "name": "@lablink/rules-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Firestore security rules tests (run via the emulator)",
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0"
  }
}
